# Troup'O Invaders Environment Configuration

# Port mapping (host:container)
# The container always runs on port 80, this maps it to your host machine
HOST_PORT=8080

# Debug mode
DEBUG=true

# Log level (for future use)
LOG_LEVEL=debug
